-- SQL datatypes
-- string data
-- char, varchar, nchar, navrchar
-- M or F, char(1)
-- varchar(256)
-- nchar(1), it supports unicode characters
-- nvarchar(256), it also supports unicode characters


-- numerical values
-- int, bigint, tinyint, decimals(p,s), smallint
-- age of the students, tinyint(255)
-- age of trees/buildings, smallint
-- decimal(p,s), p--> represents total digits, s--> no. of digits you ma have after decimal

-- -999.99, decimal(5,2)
-- date data type, time data type, datetime datatype